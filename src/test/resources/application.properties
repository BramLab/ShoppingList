spring.application.name=ShoppingList

# Alternative tables via src/test/resources/NOMOREschema.sql instead of hibernate-created ddl.
# The key change is spring.jpa.hibernate.ddl-auto=none ? this stops Hibernate from
# generating the problematic CHECK constraint entirely,
# and spring.sql.init.mode=always ensures your NOMOREschema.sql is picked up and executed instead.

#spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL
#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
#spring.sql.init.mode=always
#spring.jpa.show-sql=true