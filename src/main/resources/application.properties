spring.application.name=ShoppingList

spring.datasource.url=jdbc:mysql://localhost:3306/shoppinglist
spring.datasource.username=intec
spring.datasource.password=intec-123

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

logging.level.be.intecbrussel.shoppinglist.service.StoredFoodServiceImpl=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
# Serialize LocalDate / LocalDateTime as ISO strings ("2026-07-15") instead of
# JSON arrays ([2026,7,15]).  Without this, JavaScript === comparisons between two
# date values in the HTTP test suite always return false (different array objects).
# NO LONGER: spring.jackson.serialization.write-dates-as-timestamps=false